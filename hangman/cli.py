from __future__ import annotations

from typing import Sequence


def main(argv: Sequence[str] | None = None) -> int:
    """CLI entrypoint (stub).

    Prints a single-line message and exits successfully.
    """
    print("Hangman game (stub).")
    return 0
